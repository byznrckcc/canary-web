version: '3.8'

services:
  canary-web:
    build: .
    container_name: canary_web_container
    ports:
      - "5000:5000"
    volumes:
      - ./canary_dev.db:/app/canary_dev.db
    environment:
      - FLASK_ENV=production
    restart: unless-stopped
